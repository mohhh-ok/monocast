---
name: error-investigate
description: アプリケーションのログファイル (`.logs/app.jsonl` など) を読んで、最近のエラー・警告を抽出し「いつ・どこで・何が起きたか」を要約するだけのスキル。ソースコードを開いたり原因仮説を立てたりはしない。ユーザーが「エラー調べて」「ログ見て」「何かエラー出てる？」「アプリ落ちた」「動かない、ログ確認して」「最近のエラー」「ログにwarn出てない？」「/error-investigate」などと言ったときに発火する。コードを読まず推測もせず、ログ事実のみを返すのが本スキルの役割。
---

# error-investigate

ログを参照することだけが仕事のスキル。**コードを開かない・原因を推測しない・修正提案もしない**。ログに書かれている事実だけを抜き出してユーザーに見せる。

理由：「ログを見るだけ」と切り分けておくと、ユーザーは「まずログだけ見たい」というニーズに応えてもらえ、その上で必要なら別途コードを読みに行ける。スキルが勝手にコードまで踏み込むと、ログ確認だけしたい場面でノイズになる。

## 動作

### 1. ログファイルを探す

優先順位：

1. `.logs/app.jsonl`（このリポジトリで使われている JSONL ログ）
2. `logs/*.log`, `*.log`（プロジェクト直下）
3. `~/Library/Logs/` 配下（macOS アプリログを聞かれた場合のみ）

見つからなければ「ログファイルが見つかりません。`.logs/app.jsonl` などのパスを教えてください」と返して終了。勝手に作らない。

### 2. レコードを抽出する

JSONL なら `jq` で `level` が `error` または `warn` の行を取り出す。直近のものを優先。デフォルトは最新 50 件。

```bash
tail -n 2000 .logs/app.jsonl | jq -c 'select(.level=="error" or .level=="warn")' | tail -n 50
```

プレーンテキストログなら `grep -iE 'error|warn|exception|failed|fatal'` で拾う。

ユーザーが「今日のだけ」「直近1時間」など期間を指定したら `ts` フィールドで絞る。指定がなければ全期間から末尾 50 件。

### 3. 出力する

以下の形式でまとめる：

```
## ログ要約

参照ファイル: <パス>
抽出件数: <error N件 / warn M件>
期間: <最古 ts> 〜 <最新 ts>

### error
- [<ts>] [<tag>] <msg>  (+ extra フィールドがあれば inline で)
- ...

### warn
- [<ts>] [<tag>] <msg>
- ...
```

件数が多すぎる（>20件）なら、同じ `tag` + `msg` をまとめて「N回」とカウントする。スタックトレースなど長いフィールドは 1〜2 行に省略し、必要なら「フルを見る？」と末尾で聞く。

### 4. 終わり

ここで止める。「原因はおそらく…」「次にこれを直すと…」のような推測は **書かない**。ユーザーが「なぜこれ起きてる？」と続けて聞いてきて初めてコード調査に入る（その時点で本スキルの役割は終わっている）。

## ログ形式の参考（monocast 系プロジェクト）

`.logs/app.jsonl` の各行は `src/lib/log.ts` で書かれた JSON：

```json
{"ts":"2026-05-12T10:23:45.123+09:00","level":"error","tag":"news.rss","msg":"feed fetch failed","url":"https://...","error":"ETIMEDOUT"}
```

- `ts`: ISO8601（タイムゾーン付き）
- `level`: `debug` / `info` / `warn` / `error`
- `tag`: モジュール名（`news.rss`, `tts.voicevox` など）
- `msg`: 短い説明
- 任意の追加フィールド: `url`, `error`, `durationMs` など

`debug` / `info` はノイズになるので、明示的に求められない限り出さない。

## 禁止

- ソースコードを開いて原因を探る（このスキルの範囲外）
- ログに書かれていないことを推測して報告する
- 「次のステップ」「修正案」を勝手に提案する
- ログを書き換える・削除する
