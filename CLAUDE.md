# monocast — Claude 向け作業メモ

## 大原則：「動けばいい」精神をやめる

不具合や挙動の質問を受けたとき、症状を隠す方向に飛びつかない。

- **症状を隠す ≠ 直す**。`try/catch` で握り潰す、エラー時に勝手にスキップ／リトライする、`onError` で次に進める、404 を 200 にしてしまう、といった「動いてしまえば気付かれない」系の対処を、根本原因の特定より先に提案・実装しない。
- **観測と治療を分ける**。まず「いつ・どこで・何が起きているか」を取れる状態にする（ログ追加、`console.error` で詳細出力、再現条件の特定）。データが揃う前に挙動を変えない。
- **ユーザーの調査フェーズを尊重する**。「原因が X じゃないかと思ってる」と言われている段階で、こちらから「じゃあ Y で回避すれば直る」を出さない。原因究明に必要な情報の取り方だけ提案する。
- **回避策は最後の選択肢**。根本原因が判明したうえで、コスト・影響範囲を踏まえて選ぶもの。「とりあえず動かす」ためのデフォルト手段ではない。

要するに：**バグは隠すな、見ろ**。

## 補足

- 提案時は「観測のための変更」か「挙動を変える変更」かを明示する。
- 挙動を変える前にユーザーの同意を取る。同意を取らずに「ついでに」入れない。
