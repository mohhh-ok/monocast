# 番組とキュー

monocast の番組生成・キュー仕様。

## 番組

- 1 番組 = 10 件程度のニュースをまとめた読み上げ（尺は件数と各記事 3〜5 文のルールに比例して決まる）
- 番組メタは `data/queue.json` に永続化
- 音声は `public/audio/*.mp3` に置かれ、再生終了時に削除される
- 1 リクエストにつき生成は 1 件ずつ (二重生成防止)
